---
phase: 01-signal-durci-cap-beta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - server/routes.ts
autonomous: true
requirements:
  - FEEDBACK-02
  - FEEDBACK-03
  - CAP-01
  - CAP-03

must_haves:
  truths:
    - "GET /api/beta/capacity retourne { count, cap, isFull } en moins de 100ms"
    - "POST /api/waitlist/qualified accepte email + pricingIntent + triggerFeatures et persiste en DB"
    - "GET /api/admin/feedback-dashboard retourne les agrégats match_feedback + beta_feedback + waitlist qualifiée"
    - "La table beta_waitlist a les colonnes pricingIntent et triggerFeatures (nullable, rétro-compatible)"
    - "BETA_CAP est lu depuis process.env.BETA_CAP avec fallback 150 ; isFull est true quand count >= cap"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Drizzle schema étendu pour beta_waitlist avec champs qualifiés"
      contains: "pricingIntent"
    - path: "server/routes.ts"
      provides: "3 nouveaux endpoints : /api/beta/capacity, /api/waitlist/qualified, /api/admin/feedback-dashboard"
      exports: ["GET /api/beta/capacity", "POST /api/waitlist/qualified", "GET /api/admin/feedback-dashboard"]
  key_links:
    - from: "server/routes.ts GET /api/beta/capacity"
      to: "form_submissions"
      via: "SELECT count(distinct email)"
      pattern: "countDistinct.*email"
    - from: "server/routes.ts POST /api/waitlist/qualified"
      to: "betaWaitlist"
      via: "db.insert(betaWaitlist).values(...)"
      pattern: "pricingIntent|triggerFeatures"
---

<objective>
Poser la couche données et API dont les plans UI (02 et 03) ont besoin :
- Extension de beta_waitlist avec les champs qualification (intention de payer, features déclencheuses)
- Endpoint GET /api/beta/capacity — compteur d'emails uniques vs cap configurable
- Endpoint POST /api/waitlist/qualified — capture la waitlist enrichie
- Endpoint GET /api/admin/feedback-dashboard — agrégats pour l'admin

Purpose: Sans cette couche, les composants UI des plans suivants n'ont pas de données fiables à afficher. C'est le socle partagé de toute la phase.
Output: 2 fichiers modifiés (schema.ts + routes.ts), 3 nouveaux endpoints testables via curl.
</objective>

<execution_context>
@C:/Users/oscar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oscar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@shared/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre le schema Drizzle beta_waitlist + migration SQL</name>
  <files>shared/schema.ts</files>
  <action>
Dans `shared/schema.ts`, modifier la table `betaWaitlist` pour ajouter deux colonnes nullables rétro-compatibles :

```typescript
pricingIntent: text("pricing_intent"),  // ex: "5-10", "10-20", "20+", "non_sur"
triggerFeatures: text("trigger_features").array(), // ex: ["prospecting", "suivi_deadline", "pdf_avance"]
```

Ajouter également les types exports :

```typescript
export const insertBetaWaitlistSchema = createInsertSchema(betaWaitlist).omit({ id: true, createdAt: true });
export type InsertBetaWaitlistEntry = z.infer<typeof insertBetaWaitlistSchema>;
```

Si `createInsertSchema` n'est pas déjà importé depuis `drizzle-zod`, ajouter l'import.

IMPORTANT : les deux nouvelles colonnes sont nullable (pas de `.notNull()`). Les anciens enregistrements sans ces champs restent valides. Aucun `DEFAULT` requis.

Après modification, exécuter la migration DDL manuellement via la console Supabase SQL ou via Drizzle push. Inclure un commentaire en tête du bloc betaWaitlist expliquant l'extension v1.1.

Commande pour appliquer le changement de schéma en base (à exécuter dans le terminal) :
```
npx drizzle-kit push
```
Si la commande échoue (env Railway absent en local), documenter la migration SQL équivalente dans un commentaire au-dessus de la table :
```sql
-- Migration v1.1: ajout champs qualification waitlist
ALTER TABLE beta_waitlist ADD COLUMN IF NOT EXISTS pricing_intent TEXT;
ALTER TABLE beta_waitlist ADD COLUMN IF NOT EXISTS trigger_features TEXT[];
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "schema.ts|error" | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <done>shared/schema.ts compile sans erreur TS ; betaWaitlist contient pricingIntent et triggerFeatures ; insertBetaWaitlistSchema est exporté.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter les 3 endpoints dans routes.ts</name>
  <files>server/routes.ts</files>
  <action>
**ÉTAPE PRÉALABLE OBLIGATOIRE — Ajouter `formSubmissions` à l'import ligne 3**

L'import actuel de `@shared/schema` en ligne 3 de `server/routes.ts` est :
```typescript
import { insertFormSubmissionSchema, userFormInputSchema, matchFeedback, betaFeedback, betaWaitlist, type InsertGrant, type Grant, type GrantResult, type FormSubmission } from "@shared/schema";
```

`formSubmissions` (la table Drizzle, pas le type `FormSubmission`) n'y figure pas. Les nouveaux endpoints en ont besoin. Modifier cette ligne pour l'ajouter :
```typescript
import { insertFormSubmissionSchema, userFormInputSchema, matchFeedback, betaFeedback, betaWaitlist, formSubmissions, type InsertGrant, type Grant, type GrantResult, type FormSubmission } from "@shared/schema";
```

Vérifier que `formSubmissions` est bien exporté depuis `shared/schema.ts` (c'est la table Drizzle définie via `pgTable("form_submissions", ...)`). Si le nom d'export est différent (par exemple `formSubmission` sans 's'), utiliser le nom exact.

---

Ajouter trois nouveaux endpoints dans `server/routes.ts`, juste avant la route `app.post("/api/create-checkout-session", ...)` (vers la ligne 733) :

**Endpoint 1 — GET /api/beta/capacity**
```typescript
app.get("/api/beta/capacity", async (req, res) => {
  try {
    const { db } = await import("./db");
    const { countDistinct } = await import("drizzle-orm");
    const cap = parseInt(process.env.BETA_CAP ?? "150", 10);
    const [row] = await db
      .select({ count: countDistinct(formSubmissions.email) })
      .from(formSubmissions);
    const count = Number(row?.count ?? 0);
    res.json({ count, cap, isFull: count >= cap });
  } catch (error: any) {
    console.error("Erreur beta/capacity:", error);
    res.status(500).json({ error: error.message });
  }
});
```

Note : si `countDistinct` n'est pas disponible dans la version de drizzle-orm installée, utiliser `sql<number>\`count(distinct email)\`` comme fallback.

**Endpoint 2 — POST /api/waitlist/qualified** (avec rate limiter partagé `waitlistLimiter`)
```typescript
app.post("/api/waitlist/qualified", waitlistLimiter, async (req, res) => {
  try {
    const { email, source, pricingIntent, triggerFeatures } = req.body;
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!email || typeof email !== "string" || !emailRegex.test(email)) {
      return res.status(400).json({ error: "Email invalide" });
    }
    const { db } = await import("./db");
    await db
      .insert(betaWaitlist)
      .values({
        email: email.toLowerCase().trim().slice(0, 320),
        source: source ? String(source).slice(0, 80) : "qualified-form",
        pricingIntent: pricingIntent ? String(pricingIntent).slice(0, 20) : null,
        triggerFeatures: Array.isArray(triggerFeatures)
          ? triggerFeatures.map((f: unknown) => String(f).slice(0, 50)).slice(0, 10)
          : null,
      })
      .onConflictDoNothing();
    console.log(`📬 Waitlist qualifiée: ${maskEmail(email)} | prix: ${pricingIntent} | features: ${triggerFeatures}`);
    res.json({ success: true });
  } catch (error: any) {
    console.error("Erreur waitlist qualifiée:", error);
    res.status(500).json({ error: error.message });
  }
});
```

**Endpoint 3 — GET /api/admin/feedback-dashboard** (protégé par requireAdmin)
```typescript
app.get("/api/admin/feedback-dashboard", requireAdmin, async (req, res) => {
  try {
    const { db } = await import("./db");
    const { desc, count, sql, countDistinct } = await import("drizzle-orm");

    // match_feedback : votes par rating
    const matchVotes = await db
      .select({ rating: matchFeedback.rating, total: count() })
      .from(matchFeedback)
      .groupBy(matchFeedback.rating);

    // beta_feedback : 20 derniers
    const recentBetaFeedback = await db
      .select()
      .from(betaFeedback)
      .orderBy(desc(betaFeedback.createdAt))
      .limit(20);

    // waitlist qualifiée : toutes les entrées avec pricingIntent non null
    const qualifiedWaitlist = await db
      .select()
      .from(betaWaitlist)
      .orderBy(desc(betaWaitlist.createdAt))
      .limit(200);

    // cap courant
    const cap = parseInt(process.env.BETA_CAP ?? "150", 10);
    const [capRow] = await db
      .select({ count: countDistinct(formSubmissions.email) })
      .from(formSubmissions);
    const betaCount = Number(capRow?.count ?? 0);

    // taux utilisation match_feedback (votes / soumissions)
    const totalVotes = matchVotes.reduce((s, r) => s + Number(r.total), 0);

    res.json({
      betaCapacity: { count: betaCount, cap, isFull: betaCount >= cap },
      matchFeedback: { byRating: matchVotes, totalVotes },
      recentBetaFeedback,
      qualifiedWaitlist,
    });
  } catch (error: any) {
    console.error("Erreur feedback-dashboard:", error);
    res.status(500).json({ error: error.message });
  }
});
```
  </action>
  <verify>
    <automated>curl -s http://localhost:5000/api/beta/capacity | grep -E '"count"|"cap"|"isFull"' && echo "capacity OK"</automated>
  </verify>
  <done>
  - L'import ligne 3 de routes.ts contient `formSubmissions`
  - GET /api/beta/capacity répond { count: N, cap: 150, isFull: false } sans auth
  - POST /api/waitlist/qualified avec body {"email":"test@ex.com","pricingIntent":"5-10","triggerFeatures":["pdf_avance"]} répond { success: true }
  - GET /api/admin/feedback-dashboard avec header x-admin-token répond 200 avec betaCapacity, matchFeedback, qualifiedWaitlist
  - Le serveur démarre sans erreur (npm run dev)
  </done>
</task>

</tasks>

<verification>
```bash
# Démarrer le serveur
npm run dev &
sleep 5

# Test capacity
curl -s http://localhost:5000/api/beta/capacity

# Test waitlist qualified
curl -s -X POST http://localhost:5000/api/waitlist/qualified \
  -H "Content-Type: application/json" \
  -d '{"email":"test@integration.fr","pricingIntent":"10-20","triggerFeatures":["prospecting","suivi_deadline"]}'

# Test admin dashboard (remplacer TOKEN par la valeur de ADMIN_TOKEN dans .env)
curl -s http://localhost:5000/api/admin/feedback-dashboard \
  -H "x-admin-token: $ADMIN_TOKEN"

# Vérifier TypeScript
npx tsc --noEmit 2>&1 | grep -v "routes-old" | grep "error" | head -10
```
</verification>

<success_criteria>
- GET /api/beta/capacity retourne { count, cap, isFull } en JSON valide
- POST /api/waitlist/qualified persiste email + pricingIntent + triggerFeatures en DB
- GET /api/admin/feedback-dashboard retourne les 4 blocs de données agrégées
- BETA_CAP=1 npm run dev && curl /api/beta/capacity retourne isFull: true si au moins 1 email en DB
- Aucune régression : npx tsc --noEmit sans nouvelle erreur sur les fichiers modifiés
</success_criteria>

<output>
After completion, create `.planning/phases/01-signal-durci-cap-beta/01-01-SUMMARY.md`
</output>
