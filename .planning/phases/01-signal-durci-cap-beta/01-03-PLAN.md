---
phase: 01-signal-durci-cap-beta
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/src/pages/Home.tsx
  - client/src/pages/FormWizard.tsx
autonomous: true
requirements:
  - FEEDBACK-02
  - CAP-01
  - CAP-02
  - CAP-03

must_haves:
  truths:
    - "La homepage affiche un compteur 'X / 150 places' en temps réel qui se met à jour à chaque chargement"
    - "La page /form affiche le même compteur dans son header"
    - "Quand isFull est true, le formulaire affiche la waitlist qualifiée à la place de la step email/submit"
    - "La waitlist qualifiée capture email + slider prix (€/mois) + cases features déclencheuses"
    - "En mode normal (isFull false), le formulaire se comporte exactement comme avant"
    - "La section BetaWaitlistSection de la homepage est remplacée par QualifiedWaitlistSection qui collecte les champs étendus"
  artifacts:
    - path: "client/src/pages/Home.tsx"
      provides: "BetaCapCounter widget + QualifiedWaitlistSection remplaçant l'ancien formulaire simple"
      contains: "BetaCapCounter"
    - path: "client/src/pages/FormWizard.tsx"
      provides: "BetaCapCounter dans le header + gate de bascule vers waitlist qualifiée quand isFull"
      contains: "isFull"
  key_links:
    - from: "BetaCapCounter"
      to: "/api/beta/capacity"
      via: "useQuery(['/api/beta/capacity'])"
      pattern: "beta/capacity"
    - from: "FormWizard cap gate"
      to: "QualifiedWaitlistForm"
      via: "if (capacityData?.isFull) return <QualifiedWaitlistForm />"
      pattern: "isFull.*QualifiedWaitlist|QualifiedWaitlist.*isFull"
    - from: "QualifiedWaitlistSection"
      to: "/api/waitlist/qualified"
      via: "fetch POST /api/waitlist/qualified avec pricingIntent + triggerFeatures"
      pattern: "waitlist/qualified"
---

<objective>
Ajouter le compteur "X / 150 places" sur la homepage et dans le header de FormWizard, remplacer la section waitlist simple par la version qualifiée, et implémenter la bascule automatique du formulaire vers waitlist quand le cap est atteint.

Purpose: CAP-01/02/03 et FEEDBACK-02 sont les livrables visibles les plus critiques du milestone : créer la rareté (compteur), capter l'intention de payer (waitlist qualifiée) et protéger les coûts (gate cap).
Output: Home.tsx et FormWizard.tsx modifiés. Aucune nouvelle route dans App.tsx nécessaire.
</objective>

<execution_context>
@C:/Users/oscar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oscar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-signal-durci-cap-beta/01-01-SUMMARY.md
@client/src/pages/Home.tsx
@client/src/pages/FormWizard.tsx
</context>

<interfaces>
<!-- Interfaces extraites du codebase existant. L'exécuteur n'a pas à explorer. -->

From server/routes.ts (plan 01-01) :
GET /api/beta/capacity → { count: number, cap: number, isFull: boolean }
POST /api/waitlist/qualified body: { email, source?, pricingIntent?, triggerFeatures?: string[] }

From client/src/pages/Home.tsx :
- Language disponible via useLanguage()
- Classes de design system : mc-card, mc-card-soft, mc-mono, mc-btn-primary, mc-btn-ghost, mc-chip, mc-chip-warn, mc-section-rule
- Variables CSS : --mc-bg, --mc-text, --mc-muted, --mc-border, --mc-primary, --mc-warn, --mc-panel, --mc-primary-soft
- BetaWaitlistSection existe actuellement en bas de Home, avec un simple input email → POST /api/waitlist
- La Home utilise useQuery de @tanstack/react-query

From client/src/pages/FormWizard.tsx :
- Wizard multi-étapes avec STEPS array (status, domain, description, type, region, email, optional)
- La step "email" contient l'input email final et le bouton submit
- Le header du wizard n'a pas de compteur actuellement
- useMutation pour POST /api/submit
- useLocation de wouter pour la navigation
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BetaCapCounter + QualifiedWaitlistSection dans Home.tsx</name>
  <files>client/src/pages/Home.tsx</files>
  <action>
**A) Composant BetaCapCounter**

Ajouter un composant `BetaCapCounter` réutilisable (peut être défini dans Home.tsx puis exporté, ou déclaré localement — préférer l'export pour la réutilisation dans FormWizard) :

```typescript
export function BetaCapCounter({ className }: { className?: string }) {
  const { data } = useQuery<{ count: number; cap: number; isFull: boolean }>({
    queryKey: ["/api/beta/capacity"],
    refetchInterval: 60_000, // rafraîchissement toutes les 60s
    staleTime: 30_000,
  });
  
  if (!data) return null;
  
  const { count, cap, isFull } = data;
  const pct = Math.min(Math.round((count / cap) * 100), 100);
  
  return (
    <div className={`inline-flex items-center gap-2 mc-mono text-xs uppercase tracking-widest ${className ?? ""}`}
      style={{ color: isFull ? "var(--mc-danger, #ef4444)" : "var(--mc-primary)" }}>
      <span style={{
        display: "inline-block",
        width: 8, height: 8, borderRadius: 999,
        background: isFull ? "var(--mc-danger, #ef4444)" : "var(--mc-primary)",
        animation: isFull ? "none" : "pulse 2s infinite",
      }} />
      {isFull
        ? "Beta complète — Rejoindre la liste d'attente"
        : `${count} / ${cap} places`}
    </div>
  );
}
```

**Intégrer BetaCapCounter dans la Home** à deux endroits :
1. Dans la hero section (juste sous le badge BETA, avant le titre principal), ajouter `<BetaCapCounter className="mb-4" />`
2. Dans la BetaWaitlistSection (en haut de la section, avant le titre "100% GRATUIT"), afficher le compteur si le data est disponible.

**B) QualifiedWaitlistSection — remplacer BetaWaitlistSection**

Renommer (ou remplacer complètement) `BetaWaitlistSection` par `QualifiedWaitlistSection` qui collecte les champs étendus. Le formulaire doit s'adapter selon l'état du cap :

- Si `!isFull` (beta encore ouverte) : afficher le formulaire existant simple (email only → POST /api/waitlist), avec le compteur en haut et mention "X places restantes"
- Si `isFull` : cacher le CTA "accéder au formulaire" et afficher uniquement le formulaire de waitlist qualifiée

Le formulaire qualifié capture :
- Email (input type="email", requis)
- Intention de prix €/mois : boutons radio avec options "< 5 €", "5–10 €", "10–20 €", "20 € +", "Pas encore décidé" (valeurs: "lt5", "5-10", "10-20", "20+", "indecis")
- Features déclencheuses : checkboxes pour au maximum 3 sélections :
  - "Matching illimité" (value: "matching_illimite")
  - "Prospecting agents / galeries / labels" (value: "prospecting")
  - "Suivi des deadlines" (value: "suivi_deadline")
  - "PDF avancé (lettre de motivation auto)" (value: "pdf_avance")
  - "Accompagnement personnalisé" (value: "accompagnement")
- Bouton submit → POST /api/waitlist/qualified avec { email, pricingIntent, triggerFeatures, source: "homepage-qualified" }

Styles : cohérents avec mc-card, mc-btn-primary, mc-mono. Les boutons radio prix peuvent utiliser mc-chip cliquables (border highlight quand sélectionné avec borderColor: "var(--mc-primary)").

Remplacer l'appel `<BetaWaitlistSection ... />` dans le composant `Home` par `<QualifiedWaitlistSection ... />`.

Supprimer (ou commenter) l'ancienne définition de `BetaWaitlistSection` si elle n'est plus utilisée.

Ne pas modifier la logique des autres sections de la homepage (hero, how-it-works, profiles, exemple de grant, footer).
  </action>
  <verify>
    <automated>grep -n "BetaCapCounter\|QualifiedWaitlistSection\|beta/capacity\|waitlist/qualified" client/src/pages/Home.tsx</automated>
  </verify>
  <done>
  - Home.tsx contient BetaCapCounter exporté qui query /api/beta/capacity
  - BetaCapCounter est affiché dans la hero section
  - QualifiedWaitlistSection remplace BetaWaitlistSection dans Home
  - Le formulaire qualifié affiche email + radio prix + checkboxes features
  - POST va vers /api/waitlist/qualified avec pricingIntent et triggerFeatures
  </done>
</task>

<task type="auto">
  <name>Task 2: Cap counter + cap gate dans FormWizard.tsx</name>
  <files>client/src/pages/FormWizard.tsx</files>
  <action>
**A) BetaCapCounter dans le header du wizard**

Importer `BetaCapCounter` depuis `Home.tsx` (ou depuis un fichier shared si l'exécuteur préfère déplacer le composant dans `client/src/components/BetaCapCounter.tsx` — dans ce cas, l'adapter en conséquence dans les deux fichiers).

Dans le header du wizard (la zone qui affiche le nom "Mecene." et les étapes), ajouter `<BetaCapCounter />` à droite du logo ou sous la navigation des étapes.

Localiser le header dans FormWizard.tsx (chercher le `<header>` ou le premier `<div>` avec le logo) et y intégrer le compteur. Exemple :

```tsx
<div className="flex items-center justify-between">
  <a href="/" className="mc-display text-lg">Mecene<span style={{ color: "var(--mc-primary)" }}>.</span></a>
  <BetaCapCounter />
</div>
```

**B) Cap gate — bascule vers waitlist quand isFull**

Ajouter une query de capacité au début du composant FormWizard :

```typescript
const { data: capacityData } = useQuery<{ count: number; cap: number; isFull: boolean }>({
  queryKey: ["/api/beta/capacity"],
  staleTime: 60_000,
});
```

Ajouter un state local pour la soumission waitlist :
```typescript
const [waitlistEmail, setWaitlistEmail] = useState("");
const [waitlistStatus, setWaitlistStatus] = useState<"idle"|"loading"|"success"|"error">("idle");
```

Au début du return (avant le rendu des étapes du wizard), ajouter une condition de bascule :

```tsx
if (capacityData?.isFull) {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center px-6"
      style={{ background: "var(--mc-bg)", color: "var(--mc-text)" }}>
      <div className="max-w-lg w-full">
        <div className="mc-chip mc-chip-warn inline-flex mc-mono text-xs uppercase tracking-widest mb-6">
          [ BETA COMPLÈTE ]
        </div>
        <h1 className="mc-display text-4xl mb-4">
          Les {capacityData.cap} places beta sont prises<span style={{ color: "var(--mc-primary)" }}>.</span>
        </h1>
        <p className="mb-8 leading-relaxed" style={{ color: "var(--mc-muted)" }}>
          Rejoignez la liste d'attente prioritaire — les premiers inscrits seront les premiers servis quand de nouvelles places s'ouvrent.
        </p>
        {waitlistStatus === "success" ? (
          <div className="mc-card p-5 flex items-center gap-3" style={{ borderColor: "var(--mc-primary)" }}>
            <Check className="w-5 h-5" style={{ color: "var(--mc-primary)" }} strokeWidth={3} />
            <p>Inscription confirmée. On vous prévient dès qu'une place se libère.</p>
          </div>
        ) : (
          <form onSubmit={async (e) => {
            e.preventDefault();
            setWaitlistStatus("loading");
            try {
              const res = await fetch("/api/waitlist/qualified", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ email: waitlistEmail, source: "form-cap-gate" }),
              });
              if (!res.ok) throw new Error("Erreur");
              setWaitlistStatus("success");
            } catch {
              setWaitlistStatus("error");
            }
          }} className="flex gap-3">
            <input
              type="email"
              required
              value={waitlistEmail}
              onChange={(e) => setWaitlistEmail(e.target.value)}
              placeholder="votre@email.com"
              className="mc-input flex-1 rounded-full px-5 py-3 text-sm"
              style={{ background: "var(--mc-panel)", border: "1px solid var(--mc-border)", color: "var(--mc-text)" }}
            />
            <button type="submit" disabled={waitlistStatus === "loading"}
              className="mc-btn-primary px-6 py-3 rounded-full text-sm mc-mono uppercase tracking-widest">
              {waitlistStatus === "loading" ? "…" : "Me prévenir"}
            </button>
          </form>
        )}
        {waitlistStatus === "error" && (
          <p className="text-sm mt-3" style={{ color: "var(--mc-danger, #ef4444)" }}>
            Une erreur s'est produite. Réessayez.
          </p>
        )}
        <p className="text-xs mt-4" style={{ color: "var(--mc-muted-2)" }}>
          Aucun spam. Email utilisé uniquement pour vous prévenir.
        </p>
      </div>
    </div>
  );
}
```

IMPORTANT : cette condition `if (capacityData?.isFull)` doit être placée APRÈS tous les hooks (useState, useQuery, useMutation, useForm…) car les hooks ne peuvent pas être conditionnels. La valeur de `capacityData` est `undefined` pendant le chargement initial, donc le formulaire s'affiche normalement tant que la réponse n'est pas reçue — c'est le comportement voulu.

S'assurer que `Check` est déjà importé depuis lucide-react (vérifié dans les imports existants).
  </action>
  <verify>
    <automated>grep -n "capacityData\|isFull\|beta/capacity\|BetaCapCounter\|cap-gate" client/src/pages/FormWizard.tsx</automated>
  </verify>
  <done>
  - FormWizard.tsx contient une useQuery vers /api/beta/capacity
  - Le header du wizard affiche BetaCapCounter (ou le compteur intégré)
  - Quand isFull est true, le wizard affiche l'écran de cap gate avec formulaire email waitlist
  - Quand isFull est false ou undefined, le wizard s'affiche normalement sans régression
  - Tester avec BETA_CAP=1 en local : npm run dev → aller sur /form → l'écran cap gate doit apparaître
  </done>
</task>

</tasks>

<verification>
```bash
# Vérifier les modifications Home
grep -n "BetaCapCounter\|QualifiedWaitlistSection\|isFull\|beta/capacity\|waitlist/qualified" \
  client/src/pages/Home.tsx

# Vérifier les modifications FormWizard
grep -n "BetaCapCounter\|capacityData\|isFull\|cap-gate" \
  client/src/pages/FormWizard.tsx

# Build sans erreur
npm run build 2>&1 | tail -30

# Test cap gate : démarrer avec BETA_CAP=1 et vérifier que /form affiche l'écran waitlist
BETA_CAP=1 npm run dev &
sleep 5
curl -s http://localhost:5000/api/beta/capacity
# → doit retourner isFull: true si au moins 1 email dans form_submissions
```
</verification>

<success_criteria>
- Homepage affiche "N / 150 places" avec point pulsant vert (ou rouge si plein)
- La section waitlist de la homepage capture email + prix + features et POST vers /api/waitlist/qualified
- Header de /form affiche le même compteur N / 150
- Avec BETA_CAP=1 : /form affiche l'écran "beta complète" au lieu du formulaire
- Avec BETA_CAP=150 (défaut) : /form se comporte exactement comme avant (aucune régression)
- npm run build passe sans erreur
</success_criteria>

<output>
After completion, create `.planning/phases/01-signal-durci-cap-beta/01-03-SUMMARY.md`
</output>
